@model ProductModelo

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="card">
    <div class="card-header">
        Eliminar un Empleado
    </div>
    <div class="card-body">
        <form asp-action="Delete" asp-controller="Product" method="post">
            <input type="hidden" asp-for="IdRecurso" class="form-control" aria-describedby="emailHelp">

            <div class="alert alert-danger" role="alert">
                ¿Desea eliminar: @Html.DisplayTextFor(m => m.Title) ?
            </div>

            <button type="submit" class="btn btn-danger">Submit</button>
            <a asp-action="List" asp-controller="Product" class="btn btn-warning">Volver a la lista</a>
        </form>
    </div>
</div>